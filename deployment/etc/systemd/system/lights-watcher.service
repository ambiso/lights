[Unit]
Description=lights restarter
After=network.target

[Service]
Type=simple
ExecStart=/bin/bash -c 'cd /home/pi/lights; cargo build --release; systemctl restart lights.service'

[Install]
WantedBy=multi-user.target

